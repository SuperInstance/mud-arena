# MUD Arena dependencies
# Core (no external deps needed for CPU-only)
websockets>=12.0
aiohttp>=3.9
jinja2>=3.1

# Optional: LLM scenario generation
openai>=1.0

# Optional: visualization
matplotlib>=3.8
